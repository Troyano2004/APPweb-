package com.erwin.backend.repository;

import com.erwin.backend.entities.ComisionFormativa;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ComisionFormativaRepository extends JpaRepository<ComisionFormativa, Integer> {
}
